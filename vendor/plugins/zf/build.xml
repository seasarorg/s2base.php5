<?xml version="1.0"?>
<project name="project" default="command" basedir=".">
    <target name="command">
        <php expression="require_once('vendor/plugins/zf/config/environment.inc.php')"/>
        <command>
            <fileset dir="${plugins.dir}/zf/commands">
               <include name="*Command*.php"/>
            </fileset>
        </command>
    </target>

    <target name="test">
        <php expression="require_once('vendor/plugins/zf/config/environment.inc.php')"/>
        <echo>test directory : test/${td}</echo>
        <echo>test target : ${tt}</echo>
        <phpunit2 haltonfailure="false" haltonerror="false">
            <formatter type="plain" usefile="false"/>
            <batchtest>
                <fileset dir="test/${td}">
                    <include name="${tt}"/>
                </fileset>
            </batchtest>
        </phpunit2>
    </target>
</project>

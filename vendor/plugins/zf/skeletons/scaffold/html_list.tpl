<img src="{$base_url}/images/seasar_logo_blue.gif" alt="The Seasar Project" height="180" width="390"/>
<h2>
Generated by S2Base.PHP5
</h2>

<font color="pink">
{foreach from=$errors.validate item=val key=key}
{$key|escape} : {$val.msg|escape}
{/foreach}
</font>
<br>

<h3>
Results {$helper->getOffset()+1} - {$helper->getCurrentLastOffset()+1} of {$helper->getCount()}
</h3>

<center>
<table class="list">
<caption align="top">
<a href="{$ctl_url}/@@ACTION_NAME@@-create">create</a>
</caption>
<tbody>
@@PROPERTY_ROWS_TITLE@@
{foreach from=$dtos item=row}
  @@PROPERTY_ROWS@@
{/foreach}
</tbody>
</table>
<br>

{if $helper->isPrev()}
  <a href="{$act_url}/s2pager_offset/{$helper->getPrevOffset()}">Previous</a>
{/if}
{if $helper->getLastPageIndex()>0}
  {foreach from=$pageIndex item=index}
    {if $index == $helper->getPageIndex()}
      {$index+1}
    {else}
      <a href="{$act_url}/s2pager_offset/{$index*$helper->getLimit()}">{$index+1}</a>
    {/if}
  {/foreach}
{/if}
{if $helper->isNext()}
  <a href="{$act_url}/s2pager_offset/{$helper->getNextOffset()}">Next</a>
{/if}
<br>
<br>

<form action="{$act_url}" method="post">
<table class="list">
<tbody>
<tr>
<td>current keyword</td>
<td>{$keyword|escape}</td>
</tr>
<tr>
<td><input type="text" name="s2base_keyword" value=""/></td>
<td><input type="submit" name="" value="set keyword"/></td>
</tr>
</tbody>
</table>
</form>

</center>

Total {$helper->getLastPageIndex()+1} pages. List {$helper->getLimit()} items per page.

<h2></h2>
